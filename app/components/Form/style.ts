import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginHorizontal: 20,
    flex: 1,
  },
  headerText: {
    color: 'black',
    fontWeight: '600',
    fontSize: 32,
    marginBottom: 12,
  },
  topRow: {
    marginBottom: 28,
  },
  subHeaderText: {
    color: 'gray',
    fontSize: 20,
    marginBottom: 12,
  },
});

export default styles;
